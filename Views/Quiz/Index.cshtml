@model IEnumerable<OnlineLearningApp.Models.Quiz>

@{
    ViewData["Title"] = "List of Quizzes";
}

<div class="container mt-4">
    <h2 class="text-center mb-4">List of Quizzes</h2>
    <div class="row">
        <div class="col-md-10 offset-md-1">
            <table class="table table-striped table-hover">
                <thead>
                    <tr class="text-center">
                        <th>@Html.DisplayNameFor(model => model.QuizName)</th>
                        <th>@Html.DisplayNameFor(model => model.Description)</th>
                        <th>@Html.DisplayNameFor(model => model.DateCreated)</th>
                        <th>@Html.DisplayNameFor(model => model.ModuleId)</th>
                        @if (User?.Identity?.IsAuthenticated == true && User.IsInRole("Admin"))
                        {
                            <th>Actions</th>
                        }
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model)
                    {
                        <tr class="text-center">
                            <td>@Html.DisplayFor(modelItem => item.QuizName)</td>
                            <td>@Html.DisplayFor(modelItem => item.Description)</td>
                            <td>@Html.DisplayFor(modelItem => item.DateCreated)</td>
                            <td>@Html.DisplayFor(modelItem => item.ModuleId)</td>
                            @if (User?.Identity?.IsAuthenticated == true && User.IsInRole("Admin"))
                            {
                                <td>
                                    <a class="btn btn-outline-primary btn-sm" asp-action="Edit" asp-route-id="@item.QuizId"><i class="bi bi-pencil-square"></i> Edit</a>
                                    <a class="btn btn-outline-info btn-sm" asp-action="Details" asp-route-id="@item.QuizId"><i class="bi bi-eye"></i> Details</a>
                                    <a class="btn btn-danger btn-sm text-white" asp-action="Delete" asp-route-id="@item.QuizId"><i class="bi bi-trash"></i> Delete</a>
                                </td>
                            }
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>